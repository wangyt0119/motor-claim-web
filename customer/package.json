{
  "name": "motor-claim-customer",
  "private": true,
  "scripts": {
    "start": "npm --prefix .. run start:customer",
    "build": "npm --prefix .. run build:customer",
    "test": "npm --prefix .. run test"
  }
}
